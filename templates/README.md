# Templates

Status: **placeholder**.

Repo skeletons per component `type`, used by a future `scaffold-component` skill and by
hand. Each template should produce a repo that already satisfies the applicable
standards: `component.yaml`, `CLAUDE.md`, a minimal `pom.xml` inheriting `tn-parent`,
the `.claude/settings.json` plugin reference, `.gitignore`, and the standard GitHub
Actions workflows.

Planned:

- `java-library/`
- `java-spring-service/`
- `java-cli/`
- `react-web/`
