# Architecture

Status: **placeholder**.

Cross-cutting architecture notes for the `tn` layer and the projects above it:
the two-tier repo topology, how specs in a `*-platform` repo drive components and
`tn` capability requests, deployment shape on Kubernetes, and Architecture Decision
Records.

The current reference is the topology discussion that produced this repo; capture the
diagram and the key decisions here.
